#include <iostream>
#include <string>
using namespace std;
int main() {
    string str;
    cout«"Введите строку: "
    cin » str;
    for (int i = s.length() - 1; i >= 0; i--) {
        cout « s[i];
    }
    return 0;
}